spring:
  application:
    name: eureka-server

  cloud:
     config:
       uri: http://localhost:9000

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:9001/
